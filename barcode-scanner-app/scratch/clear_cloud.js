const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json'); // I don't have this.

// Wait, I can't use admin SDK easily. I'll use the firebase CLI or just the app code.
